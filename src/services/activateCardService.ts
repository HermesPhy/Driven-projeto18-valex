import Cryptr from "cryptr";
import bcrypt from "bcrypt";
import dotenv from "dotenv";
dotenv.config();

export default async function activateCardService(card, cvc: string, password: string) {
    
}